<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.DictionaryApk" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:textSize">@dimen/text_size</item>
    </style>

    <style name="Theme.DictionaryApk" parent="Base.Theme.DictionaryApk"/>

    <style name="Widget.DictionaryApk.Button.Icon.FilledTonal" parent="Widget.Material3.Button.IconButton.Filled.Tonal">
        <item name="android:textSize">0sp</item>
        <item name="android:drawableTint">?attr/colorOnSecondaryContainer</item>
    </style>

</resources>
